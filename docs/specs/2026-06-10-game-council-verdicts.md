

████ EXPERTO: GAME FEEL / JUICE ████
VEREDICTO: El feel de dovo está clínicamente muerto, y la autopsia es rápida: el comentario en `app/globals.css` L159 lo confiesa — "Animaciones globales (editorial, sutiles)". Construyeron un videojuego de combate entre dúos y lo vistieron de pie de foto. El síntoma más grave no es estético, es de plomería: `crearCheckin` devuelve `{ puntos }` y nada más. El cliente literalmente NO PUEDE saber que subiste de nivel, que cruzaste de Master a Legend, que tu racha avanzó. No hay juice posible sin payload — es como pedirle a un baterista que toque sin baquetas. Mientras eso no cambie, todo lo demás es maquillaje.

Lo segundo: la escala. Tienen el vocabulario correcto (pop, shake, frost, shield-flash, float-away, barras desde 0, stagger — todo bien gateado por reduced-motion) pero en dosis homeopáticas: fade-up de 10px, shake de ±7px en un banner de 11px, el tier más aspiracional del sistema renderizado a 9px y 30% de opacidad. El "−10" de un golpe entre dúos tiene la misma jerarquía visual que un toast de "guardado". El "vs" del marcador — el corazón del conflicto — está al 30%. Ganar un duelo de 7 días es un `router.refresh()`. Eso no es restraint, es anorexia de feedback.

Lo tercero: celebran lo único que celebran (el +pts flotante, decente) y lo desconectan del personaje: las barras del CharacterCard se actualizan mudas vía refresh. El golpe nunca conecta con el cuerpo.

La buena noticia es real: la infraestructura es de primera. CSS puro, tokens theme-reactivos para 6 stats, reduced-motion resuelto con el patrón correcto, un useCountUp ya escrito, y el loop de munición-por-entrenar es de los acoples mecánicos más elegantes que he visto en un producto de hábitos. No hay que demoler nada: hay que subir el volumen 10x en los momentos correctos y dejar el chrome calmado. Hit-stop, payload de juego, barras fantasma, una ceremonia L que se gane su rareza — y dovo deja de leerse como revista y empieza a sentirse como pelea.

DIRECTIVAS:
[P0] Plomería primero: crearCheckin debe devolver el payload de juego completo
  Sin datos no hay juice. Extender el retorno de la action a { puntos, deltas: {fue,res,flex,vel,equ,vit}, nivelAntes, nivelDespues, tierUps: [{stat, tierAntes, tierNuevo}], rachaPersonal, esPrimerCheckinHoy (= munición de ataque desbloqueada) }. El server ya calcula todo esto en characterSheet/apply_checkin — solo hay que devolverlo. Esta directiva BLOQUEA a todas las demás celebraciones: implementarla antes que cualquier animación.
  → lib/actions/checkins.ts, lib/leveling/* (exponer characterSheet antes/después), app/_components/CheckinRow.tsx (consumir el payload)
[P0] Tokens de motion en @theme + tokenizar el panel oscuro (matar las 7 copias inline)
  En @theme de app/globals.css: --ease-out: cubic-bezier(0.16,1,0.3,1) (la house curve, formalizada); --ease-back: cubic-bezier(0.34,1.56,0.64,1) (overshoot ~10%, SOLO en transform, nunca color/opacity); --ease-spring: linear(...) generado en kvin.me/css-springs (stiffness ~180, damping ~12) con fallback a --ease-back vía @supports (animation-timing-function: linear(0,1)); duraciones: --t-tap: 90ms, --t-fast: 180ms, --t-base: 240ms, --t-exit: 190ms (exits 20% más rápidos), --t-celebr: 900ms. Además, utility .panel-game con el radial-gradient(130% 150% at 12% 0%, #16132a 0%, #0b0a14 55%, #07060d 100%) + box-shadow 0 24px 60px -28px rgba(109,74,255,0.55) y reemplazar las 7 copias inline.
  → app/globals.css (@theme + @utility), CharacterCard.tsx, DuoChampion.tsx, home-authed.tsx, DuelScoreboard.tsx, ProBadge.tsx, ShowcaseCharacterCard.tsx, página de recompensas y nutrición
[P0] Check-in con hit-stop de 90ms y deltas por stat — el tap más importante de la app
  Secuencia exacta al res.ok: (1) el botón queda congelado en estado pressed 90ms (setTimeout — el hit-stop de Vlambeer: cambio apenas visible que se siente completamente distinto); (2) soltar TODO junto: '+N pts' sube a text-2xl con scale(clamp(1, 1 + pts/1000, 1.25)) estilo Balatro, float-away alargado a 900ms; (3) debajo, micro-floats '+12 FUE' '+5 RES' cada uno en su var(--stat-*), staggered 60ms entre sí, 700ms; (4) tick háptico navigator.vibrate?.(12) Android-only; (5) si el payload trae tierUp o levelUp → escalar a la ceremonia L (directiva 5) en vez de sumar efectos aquí. Reduced-motion: los deltas aparecen estáticos 2s y se van con fade.
  → app/_components/CheckinRow.tsx, app/globals.css (keyframes anim-float-away ampliado + nueva anim-delta-float), lib/juice.ts (helper buzz)
[P0] Barra de XP de dos capas (ghost bar) en home + flash de tier en las 6 barras
  Llevar la barra de XP al CharacterCard de la home (hoy solo existe en /perfil a 1px). Patrón 'provisional damage': fill principal salta al valor nuevo con --ease-spring 450ms; detrás, ghost bar del mismo color al 30% de opacidad que drena con delay 400ms y transición 800ms linear — el ojo lee cuánto ganaste. Aplicar el mismo patrón a las 6 barras de stats cuando llegue un delta del check-in. Al cruzar tier: flash box-shadow 0 0 0 4px color-mix(in srgb, var(--stat-X) 40%, transparent) 600ms + el label del tier sube de text-[9px] white/30 a text-[11px] al 100% con anim-pop — el logro deja de ser pie de foto. Barras con transform: scaleX (compositor), no width.
  → app/_components/CharacterCard.tsx (L90–97 los tiers, las 6 barras), app/_components/StatBar.tsx, app/(app)/perfil (reusar el mismo componente de barra)
[P0] Ceremonia de level-up / tier-up: la capa L que hoy no existe
  Nuevo client island LevelUpDialog con <dialog>.showModal() + @starting-style { scale: 0.8; opacity: 0 } + transition-behavior: allow-discrete, entrada 300ms --ease-back. Secuencia WAAPI con await animation.finished: ANTICIPACIÓN 350ms (::backdrop sube a rgba(7,6,13,0.85), nivel viejo en pantalla) → HIT-STOP 80ms → CLÍMAX: número nuevo entra scale(3)→1 con --ease-back 500ms + count-up + burst de 16 partículas DOM (spans con transform/opacity, color --c-signal) + buzz([40,40,90]) → SETTLE 400ms: clase/tier nuevo + CTA 'presumir' (share card). Total ≤2.5s, skippable con un tap. Tier-up a Master/Legend usa la misma ceremonia con el color del stat. Reduced-motion: dialog estático con el dato y el CTA. Gating duro: máximo UNA ceremonia L por visita; si coinciden level-up y tier-up, se encadenan en el mismo dialog.
  → nuevo app/_components/LevelUpDialog.tsx, app/globals.css (@starting-style + keyframes), disparado desde CheckinRow.tsx con el payload de la directiva 1
[P1] Ataques con drama: tensión de 800ms, shake del marcador y countdown de congelamiento
  AttackPanel: al lanzar, estado 'lanzando…' con pulso 800ms ANTES del resultado (la recompensa variable del escudo necesita tensión — hoy el reveal es instantáneo y mata el gambling del sistema). Golpe conectado: hit-stop 100ms → shake del DuelScoreboard completo (no del banner) con anim-shake a ±5px → '−10' en text-3xl display font-black con textShadow 0 0 24px color-mix(in srgb, var(--stat-flex) 55%, transparent), float-away 900ms → count-up del marcador rival hacia abajo. Escudo bloqueado: anim-shield-flash existente pero en el scoreboard + copy MX ('traían escudo, compa'). Congelamiento: chip con countdown real desde congela_hasta ('descongela en 7h 12m', mono text-xs, refresh por minuto) + anim-frost. Subir los botones de ataque de pills mono 11px a .btn-game (directiva 9) con icono propio.
  → app/(app)/retos/_components/AttackPanel.tsx, app/_components/DuelScoreboard.tsx, app/(app)/retos/_components/DuelFeed.tsx
[P1] La víctima DEBE enterarse: superficie del ataque recibido en home y nav
  Si hubo golpe/congelamiento en las últimas 24h: (1) banner de impacto arriba del héroe en home-authed.tsx — entra con anim-shake una sola vez, fondo color-mix(in srgb, var(--stat-flex) 12%, transparent), borde izquierdo 3px del mismo color, copy MX 'te metieron un golpe 🥊 — cóbratela' con link a /retos; (2) dot badge de 6px bg-signal con anim-pop junto al link 'retos' en AppNav (primer badge del nav — hoy hay CERO indicadores de estado); (3) si TU usuario está congelado: overlay de escarcha en el CharacterCard — borde var(--stat-vel) + anim-frost + countdown. El drama PvP que hoy muere dentro de /retos pasa a perseguirte a la home: eso ES el loop de contraataque.
  → app/_components/home-authed.tsx, app/_components/AppNav.tsx, app/_components/CharacterCard.tsx
[P1] Ceremonia W/L de duelo: nadie gana una guerra de 7 días con un router.refresh
  Al cerrar duelo, pantalla de resultado takeover (dialog full-screen o ruta /retos/[id]/resultado): tratamiento letter-grade estilo ZZZ — 'GANARON' / 'PERDIERON' en clamp(72px, 20vw, 120px) weight 900 con -webkit-text-stroke 2px var(--c-signal), entrada scale(2.4)→1 con --ease-back 500ms; count-up del marcador final 800ms; en victoria: confetti canvas (canvas-confetti lazy-imported, particleCount ≤ 80, useWorker: true, disableForReducedMotion: true — la única micro-dep autorizada); en derrota: NO shake (el shake es para impactos, no para tristeza) — desaturación grayscale(0.6) del marcador + copy directo a la revancha. Cierre con dos CTAs: 'revancha' (pre-crea el reto contra el mismo dúo) y 'presumir' (share card 9:16 que ya existe en landing.css .share-mock). El historial de retos enlaza a esta pantalla.
  → app/(app)/retos/page.tsx (acción cerrar duelo), nuevo componente DuelResultScreen, lib/actions/retos.ts (devolver resultado al cerrar), package.json (canvas-confetti, import() dinámico)
[P1] Botón físico .btn-game para las acciones de juego (la física de Brawl Stars sin su paleta)
  Nueva @utility btn-game: background var(--c-signal); box-shadow 0 5px 0 color-mix(in oklch, var(--c-signal), black 35%) (bisel duro); :active { translate: 0 4px; box-shadow: 0 1px 0 ... } con transition 80ms ease-out — el botón se HUNDE, respuesta <100ms (regla Swink). Al soltar, rebote translate 0 0 con --ease-back 250ms. Aplicar SOLO a acciones de juego: quickLog del check-in, lanzar golpe/congelar, aceptar reto, dar boost, reclamar recompensa. Los pills editoriales (rounded-full bg-ink) se quedan para navegación y forms — esa separación ES la jerarquía: chrome calmado, juego físico. Reduced-motion: el hundimiento se mantiene (es feedback de input, no decoración).
  → app/globals.css (@utility btn-game), CheckinRow.tsx, AttackPanel.tsx, RetoNuevoForm.tsx, BoostButton.tsx, página de recompensas
[P1] Matriz de gating S/M/L: el presupuesto de juice que evita la fatiga
  Documentar como comentario-contrato en globals.css (sustituyendo el manifiesto 'editorial, sutiles' de L159) y hacerla cumplir: CAPA S (<400ms, inline, sin partículas): check-in normal, log de ejercicio, dar/recibir boost, copiar cupón. CAPA M (600–1200ms, dentro del card, máx 20 partículas DOM): golpe conectado/recibido, racha semanal +1, sesión del día completa, recompensa desbloqueada, subir posición en leaderboard. CAPA L (1.5–2.5s, takeover, confetti canvas, máx 1 por visita): level-up, tier-up Master/Legend, W/L de duelo, milestone de racha de dúo (4/8/12/24/52), pacto sellado. PROHIBIDO: shake en eventos positivos, partículas en capa S, dos L encadenadas (se hacen cola), jitter perpetuo en más de 1 elemento por viewport. La capa L se gana siendo rara — es la técnica #1 de Duolingo (+1.7% D7 con UNA animación de hito).
  → app/globals.css L159 (reemplazar el comentario manifiesto), docs/specs/DESIGN.md (sección nueva 'Juice budget')
[P2] Capa físico-sonora: háptica Android + audio sintetizado opt-in
  lib/juice.ts con tres piezas: (1) buzz = (p) => navigator.vibrate?.(p) — tick 12ms en check-in, [40,40,90] en level-up/victoria, 60ms al recibir golpe; iOS no vibra jamás (hack del switch parchado en iOS 26.5): ahí el golpe se comunica solo con shake+flash, nunca diseñar dependiendo de la háptica. (2) AudioContext singleton con resume() en el primer tap; sonidos por OscillatorNode (cero assets): blip 880Hz/60ms sine en check-in, sweep 220→110Hz/120ms al recibir golpe, arpegio 523-659-784Hz en level-up; pitch aleatorio ±8% por reproducción (anti-fatiga Vlambeer). (3) Toggle 'sonido' en /ajustes, default OFF — es web, la gente lo abre en el camión. El audio nunca es el único canal de feedback (iOS lo mata con el switch físico en silencio).
  → nuevo lib/juice.ts, app/(app)/ajustes (toggle persistente), CheckinRow.tsx, AttackPanel.tsx, LevelUpDialog.tsx
[P2] Count-up universal: extraer useCountUp y dar a los números su rol de protagonistas
  El useCountUp de DuelScoreboard (rAF, ease-out cúbico, 800ms, guard de reduced-motion — ya está bien escrito) se extrae a lib/hooks/useCountUp.ts y se aplica a: nivel del CharacterCard cuando cambia, racha gigante en /recompensas, puntos en filas del leaderboard al cambiar de tab semana/mes, marcador final en la ceremonia W/L. Parámetros: 700ms, easeOutExpo, font-variant-numeric: tabular-nums SIEMPRE (evita jitter de layout), animar solo al cambiar el valor — no en cada mount. En leaderboard añadir deltas de posición ↑↓ (mono text-[10px], ↑ en var(--stat-equ), ↓ en var(--stat-flex)) comparando contra el periodo anterior. Regla Balatro: el número ES el objeto de juego — todo número que representa progreso merece moverse al cambiar; todo número estático es un dato muerto.
  → nuevo lib/hooks/useCountUp.ts, app/_components/DuelScoreboard.tsx (refactor), CharacterCard.tsx, app/(app)/recompensas/page.tsx, app/(app)/leaderboard (DuoLeaderRow)

FIRMAS: EL HIT-STOP DE 90ms COMO FIRMA FÍSICA: todo impacto en dovo (check-in registrado, golpe conectado, level-up) congela la UI 90ms entre el input y la reacción, y luego suelta TODO el feedback junto (float, flash, buzz, sonido). Nadie en producto web lo hace; cuesta un setTimeout y es la diferencia entre 'toast de guardado' y 'golpe que se siente en la mano'. Quien pruebe dovo 30 segundos lo nota sin saber por qué. | EL MARK QUE REACCIONA: las dos 'o'/discos del mark como personaje estilo Duolingo sin Rive — 4 estados CSS sobre el SVG en AppNav: idle (la respiración desfasada de 3.6s que YA existe en el landing, traída in-app), éxito (salto con squash & stretch: scaleY 1.15/scaleX 0.9 → settle con --ease-back), golpeado (shake ±3px), congelado (anim-frost + tinte --stat-vel). El dúo literalmente vive en el logo y reacciona a lo que les pasa — branding y game-feel en el mismo gesto, cero deps. | LA BARRA FANTASMA EN TODO MEDIDOR: el patrón 'provisional damage' de los fighting games aplicado a XP y a las 6 stats — el fill salta con spring al valor nuevo y una ghost bar del color del stat drena 400ms después, de modo que el ojo SIEMPRE lee 'cuánto acabas de ganar' sin leer un solo número. Convertir cada check-in en una lectura visceral de progreso es lo que conecta el loop diario con el personaje, y se vuelve el tell visual inconfundible de dovo.

████ EXPERTO: METAGAME / RETENCIÓN ████
VEREDICTO: dovo tiene una economía de juego digna de un estudio F2P serio — munición que se gana entrenando, boosts con cooldown enforced en DB, dos rachas, clases con lore — y la sirve como revista de diseño. Es un casino donde las fichas no suenan. Tres fallas estructurales, en orden de gravedad:

Primera: el meta loop semanal puede no existir. cerrar_semana_rachas no tiene scheduler en el repo; la racha de dúo es la moneda que gatea boosts y TODAS las recompensas, y nadie está verificando que se acuñe. Eso no es un problema de diseño: es un agujero en la caja registradora. Cualquier UI de racha que construyamos encima de un cron fantasma es escenografía.

Segunda: los kairos están mudos. crearCheckin devuelve {puntos} y nada más: subir de nivel es indetectable, la victoria de un duelo es un router.refresh, recibir un golpe rival no toca la home ni la nav. El jugador genera eventos de drama y la UI los archiva como pies de foto a 9px y white/30. Duolingo movió +1.7% de retención D7 rediseñando UNA animación de hito; dovo tiene cero hitos renderizados.

Tercera: "lo prometido es deuda" es un tagline sin mecánica. El modelo de castigos fue dropeado en mayo (20260520120000_drop_v0x) y nada lo reemplazó. El diferencial del producto — el trato con consecuencias — no existe en la base de datos.

La home no es un lobby: es un índice. No dice si tu racha está en riesgo (dato 100% calculable hoy con rutina + check-ins), no cuenta regresivamente hacia nada, no muestra la munición que ya ganaste. El loop diario de 90 segundos — entrenar → cargar bala → disparar → push al rival — está completo en backend y desconectado en pantalla. La buena noticia: el vocabulario de juice ya existe en globals.css y el restraint editorial es un activo, no un estorbo — el chrome se queda calmado y el drama se concentra en momentos raros y ganados. Lo que sigue no es redecorar: es cablear el videojuego que el backend ya pagó.

DIRECTIVAS:
[P0] Botín completo en crearCheckin — prerequisito de toda celebración
  Cambiar el retorno de crearCheckin de { puntos } a { puntos, deltas: Record<'fue'|'res'|'fle'|'vel'|'equ'|'vit', number>, nivelAntes, nivelDespues, tierUps: Array<{stat, de, a}>, rachaPersonal, municionLista: boolean, compliance: {hechos, meta} }. Todo se calcula server-side con characterSheet antes/después en la misma action (una query extra, cero round-trips nuevos). El cliente decide la capa de celebración (S/M/L) según el payload. Sin esto, level-up, tier-up y racha siguen siendo invisibles por construcción.
  → /home/elmikebutron/dovo/lib/actions/checkins.ts, /home/elmikebutron/dovo/app/_components/CheckinRow.tsx
[P0] Verificar/crear el scheduler de cerrar_semana_rachas
  Antes de construir UI de racha: confirmar cómo corre en prod. Si no corre, crear migración con pg_cron: cron.schedule('cerrar-semana-rachas', '59 23 * * 0', $$select cerrar_semana_rachas()$$) en zona America/Mexico_City (o Vercel Cron → route handler con service_role si pg_cron no está habilitado). El mismo job debe auto-cerrar duelos vencidos y expirar propuestas con RETO_ACEPTAR_HORAS=48 (constante declarada en lib/scoring/constants.ts y jamás aplicada). Este job ES el reloj del metajuego: el Veredicto del Domingo, las recompensas y los boosts cuelgan de él.
  → /home/elmikebutron/dovo/supabase/migrations/ (nueva), /home/elmikebutron/dovo/lib/scoring/constants.ts, /home/elmikebutron/dovo/lib/actions/retos.ts, vercel.json
[P0] TratoHUD: la home abre con el estado del trato, no con un índice
  Nuevo componente server TratoHUD.tsx insertado en home-authed.tsx ARRIBA del héroe 'hoy te toca'. Contenido: (a) compliance en vivo por miembro — 'vas 2/4 esta semana' tú y tu compa en dos columnas con segmentos llenables (scaleX, color --c-signal); (b) racha de dúo con el mark de dos discos como icono propio (la racha SIEMPRE violeta, en toda la app — consistencia ritual); (c) countdown al cierre 'el trato se juzga en 2d 14h' (domingo 23:59 CDMX, tick de 1 min, tabular-nums). Estados: al-día (signal pleno), en-riesgo si proyección < meta a 48h del cierre (filter: grayscale(.8) en el contador + pulso de opacity .75↔1 a 2.4s — loss aversion visible, jamás push de culpa), rota (estática gris + CTA de comeback 'duelo de regreso'). Los datos ya son 100% calculables con rutina default + check-ins de la semana.
  → /home/elmikebutron/dovo/app/_components/home-authed.tsx, nuevo /home/elmikebutron/dovo/app/_components/TratoHUD.tsx, query nueva en /home/elmikebutron/dovo/lib/actions/grupos.ts
[P0] Sistema de celebración por capas S/M/L con gating estricto
  Tokens nuevos en @theme de globals.css: --ease-spring y --ease-bounce como linear() (valores del generador de Jake Archibald) con fallback @supports → cubic-bezier(0.34,1.56,0.64,1). Capa S (cada check-in): mantener anim-float-away + anim-pop actuales, no tocar. Capa M (tier-up de stat, racha semanal +1, golpe conectado): 600–1200ms dentro del card — barra del stat con overshoot scaleX 1→1.04→1 en --ease-bounce + flash del color --stat-* correspondiente + burst de 12–16 partículas DOM (transform/opacity, sin física JS). Capa L SOLO para: level-up, tier Master/Legend, milestone de racha, victoria de duelo, deuda saldada — <dialog>.showModal() con @starting-style {scale:.8; opacity:0} + ::backdrop con blur, secuencia anticipación 350ms (dim) → hit-stop 80ms → clímax con canvas-confetti lazy-imported (import() dinámico, particleCount ≤80, useWorker:true, disableForReducedMotion:true — única micro-dep aprobada) → settle con count-up (rAF 700ms easeOutExpo, tabular-nums) + CTA compartir. Regla dura: si todo es jackpot nada lo es — la capa L aparece máximo 1–2 veces por semana por usuario. prefers-reduced-motion: dialog estático con el estado final, feedback informativo intacto.
  → /home/elmikebutron/dovo/app/globals.css (@theme + keyframes), nuevo /home/elmikebutron/dovo/app/_components/Celebracion.tsx (client island con <dialog>), /home/elmikebutron/dovo/app/_components/CheckinRow.tsx, /home/elmikebutron/dovo/app/_components/StatBar.tsx
[P0] El ataque recibido tiene que doler en la home
  Hoy la víctima solo se entera dentro de /retos: el drama PvP muere en silencio. Tres superficies: (1) banner en home-authed arriba del TratoHUD cuando hay golpe/congelamiento activo, montado con anim-shake existente + '−10' en float-away + countdown mm:ss derivado de congela_hasta (dato ya disponible, hoy no renderizado); (2) overlay de escarcha en CharacterCard cuando el usuario está congelado — pseudo-elemento ::after con linear-gradient blanco-azul rgba(58,134,255,.12) + anim-frost existente, el personaje literalmente congelado; (3) badge en la navegación (ver directiva del tab bar). Mismo patrón para el escudo en mano: chip 'escudo activo 🛡 5d' en TratoHUD — el dueño hoy ni sabe que lo tiene. Corregir de paso el copy falso de hintEscudo en BoostButton ('protege la racha' → 'bloquea el siguiente ataque rival').
  → /home/elmikebutron/dovo/app/_components/home-authed.tsx, /home/elmikebutron/dovo/app/_components/CharacterCard.tsx, /home/elmikebutron/dovo/app/_components/BoostButton.tsx, /home/elmikebutron/dovo/app/(app)/retos/_components/DuelFeed.tsx (reusar query), messages/es.json y en.json
[P1] El Veredicto del Domingo: ceremonia de cierre semanal
  Al primer open posterior al cierre (domingo 23:59 CDMX), takeover de una sola vez (persistir 'visto' en DB por semana): si AMBOS cumplieron → ceremonia capa M/L con count-up de la racha nueva y los dos discos del mark pulsando desfasados (reusar la lógica de dv-breathe del landing, la única animación con personalidad de marca); si alguien falló → pantalla de deuda con copy MX ('lo prometido es deuda, compa') y el castigo asignado. Es el clímax fijo del meta loop — la única pantalla donde el restraint editorial se rompe por completo. El countdown del TratoHUD apunta a este momento toda la semana: anticipación → clímax → settle a escala semanal.
  → nuevo /home/elmikebutron/dovo/app/(app)/veredicto/ o dialog en home-authed.tsx, /home/elmikebutron/dovo/lib/actions/grupos.ts, tabla nueva veredictos_vistos en supabase/migrations/
[P1] Resucitar la deuda como mecánica: tabla, catálogo y cinta de peligro
  La promesa fundacional no tiene soporte en datos. Crear tabla deudas (duo_id, deudor_id, acreedor_id, castigo_id, estado pendiente|cumplida, creada_en, cumplida_en) + catálogo castigos seed con 12–15 castigos en voz MX ('pagas los tacos del sábado', 'lavas los trastes toda la semana', 'subes story haciendo burpees'). Generación automática al fallar compliance semanal (mismo cron del Veredicto). UI: mientras haya deuda pendiente, el TratoHUD viste cinta de peligro — border-top de 8px con repeating-linear-gradient(45deg, var(--c-signal) 0 12px, transparent 12px 24px) — y chip persistente 'deuda pendiente'. Solo el ACREEDOR la marca cumplida (botón en /grupo/[id]). Regla de hierro: la misericordia (escudo/freeze) aplica a la RACHA, jamás a la deuda — pagar para absolverse mataría el producto. Al saldar → celebración capa L + share card 9:16 'deuda saldada' (reusar estética .share-mock del landing).
  → supabase/migrations/ (deudas + castigos + RLS), /home/elmikebutron/dovo/lib/actions/ (nueva deudas.ts), /home/elmikebutron/dovo/app/_components/TratoHUD.tsx, /home/elmikebutron/dovo/app/(app)/grupo/[id]/page.tsx, /home/elmikebutron/dovo/lib/supabase/types.ts (está stale, regenerar)
[P1] Duelo con tensión: barra de ventaja, head-to-head, revancha y pantalla W/L
  En DuelScoreboard: (a) barra de ventaja horizontal — dos scaleX enfrentados desde el centro, tu lado en --c-signal, rival en --stat-fle magenta, transición 700ms; (b) estado 'vas perdiendo' cuando el margen es ≥10%: tu lado a grayscale(.6) + microcopy MX ('te están comiendo, muévete'); (c) récord head-to-head persistente 'van 3–2' sobre el marcador (query al histórico de retos por par de dúos — el índice único por par ya existe). Al cierre (automático vía cron): pantalla W/L con letter grade estilo ZZZ — S/A/B según margen (S >30%, A 10–30%, B <10%), 140px font-weight 900 con -webkit-text-stroke violeta, entrada scale(3)→1 en --ease-bounce, 'vs' al 100% de opacidad (hoy está al 30%: el corazón del conflicto como pie de foto). Si el margen fue <15%, botón 'revancha' pre-llenado — Kilduff: los resultados cerrados + competencia repetida fabrican rivales, y los rivales son el motor con más evidencia del producto.
  → /home/elmikebutron/dovo/app/_components/DuelScoreboard.tsx, /home/elmikebutron/dovo/lib/actions/retos.ts, /home/elmikebutron/dovo/app/(app)/retos/page.tsx, nuevo componente ResultadoDuelo.tsx
[P1] Bottom tab bar HUD con badges de estado
  Reemplazar los 7 links de texto a 9px con overflow-x-auto por una tab bar fija inferior (position: fixed; bottom: 0; padding-bottom: env(safe-area-inset-bottom); backdrop-blur como el header actual) con 5 tabs: hoy / duelo / tabla / premios / perfil. Nutrición vive dentro de 'hoy' (mantener el FAB actual). Iconos SVG propios stroke 1.5px (sistema de 8–10 iconos que también sustituye los emojis 🥊❄️🛡️ del AttackPanel/DuelFeed — un juego tiene sprites, no emoji de sistema). Badges de estado: punto violeta pulsante en 'duelo' cuando hay munición lista sin gastar; contador en magenta cuando recibiste ataque; dot en 'premios' cuando hay recompensa recién desbloqueada. AppNav superior queda solo wordmark + ajustes + toggle de tema. A 390px la zona del pulgar deja de estar vacía: la nav ES el HUD.
  → /home/elmikebutron/dovo/app/_components/AppNav.tsx, nuevo /home/elmikebutron/dovo/app/_components/TabBar.tsx, nuevo /home/elmikebutron/dovo/app/_components/icons/, /home/elmikebutron/dovo/app/(app)/layout.tsx
[P1] Milestones de racha con animación exclusiva y share card a WhatsApp
  En las semanas 2/4/8/12/24/52 (alineadas con los thresholds de recompensas ya existentes en rewards), el Veredicto del Domingo escala a capa L con animación EXCLUSIVA del milestone (no reusar la genérica — la exclusividad es lo que la hace valiosa: Duolingo movió +1.7% D7 con una sola animación de hito). Settle con share card 9:16 reutilizando el patrón .share-mock del landing (radial violeta+magenta, racha gigante con textShadow, los dos nombres del dúo) + Web Share API (navigator.share con fallback a descarga de imagen) apuntando a WhatsApp — 5–10x sharing orgánico es el canal de growth más barato disponible. Mostrar también max_streak ('tu récord: 14 sem') que hoy jamás se renderiza.
  → /home/elmikebutron/dovo/lib/actions/rewards.ts, /home/elmikebutron/dovo/app/(app)/recompensas/page.tsx, componente Celebracion.tsx, nuevo ShareCard.tsx (canvas u og-image route)
[P2] Temporada mensual: reset del leaderboard, jamás de la progresión
  Reframe del leaderboard semana/mes como 'Temporada N' con nombre MX rotativo. Pantalla de cierre de temporada con podio (top 3 dúos con DuoChampion ampliado) y track vertical de 8–10 recompensas colgado del XP del mes — trophy road con flex + pseudo-elementos: nodos conectados por border-left que cambia de ink/15 a --c-signal al completarse, milestone foil cada 3 nodos (gradiente animado + background-blend-mode: color-dodge, estático en reduced-motion). Regla absoluta anti-FOMO 2026: la temporada resetea SOLO leaderboard y liga; XP, niveles, tiers, clases y prestige son permanentes. Aprovechar para activar prestige/Immortal (columna y display ya existen, ninguna mecánica la incrementa): prestige al cierre de temporada para quienes terminen en top X.
  → /home/elmikebutron/dovo/app/(app)/leaderboard/page.tsx, /home/elmikebutron/dovo/lib/actions/leaderboard.ts, /home/elmikebutron/dovo/lib/leveling/ (activar prestige), nueva migración de temporadas
[P2] Quest semanal cooperativa + scouting de rivales
  Dos cierres del tejido social: (1) card 'misión de la semana' en home bajo el TratoHUD — meta cooperativa aleatoria de 5 días ('5 check-ins entre los dos antes del viernes', '2 días donde ambos entrenen') con barra compartida y nudge pre-escrito en voz MX enviable por push ('no me dejes morir solo, compa') — análogo Friend Quest: seguir amigos = 5.6x más completion en Duolingo; recompensa: 1 escudo o munición extra, nunca puntos de leaderboard. (2) En RetoNuevoForm, el picker de rival hoy descarta datos que getLeaderboard YA trae: renderizar racha_duo, top_clase y top_stat de cada candidato como mini ficha de scouting (chip de clase en --c-signal, stat dominante con su color, racha con el icono ritual) — elegir rival debe sentirse como elegir oponente en un fighting game, no como un <select>. Corregir el copy stale de es.json ('id del dúo rival').
  → /home/elmikebutron/dovo/app/(app)/retos/nuevo/RetoNuevoForm.tsx, /home/elmikebutron/dovo/lib/actions/leaderboard.ts, /home/elmikebutron/dovo/app/_components/home-authed.tsx, messages/es.json y en.json, nueva tabla quests en supabase/migrations/

FIRMAS: EL VEREDICTO DEL DOMINGO — todos los domingos 23:59 CDMX el trato se juzga: una pantalla ceremonial única dicta racha +1 (los dos discos del mark pulsando desfasados) o deuda (cinta de peligro + castigo del catálogo). Ninguna app de fitness tiene un clímax semanal a hora fija; dovo se convierte en el juzgado del trato y toda la semana cuenta regresivamente hacia ese momento. | MUNICIÓN QUE ARDE — el check-in de hoy carga visiblemente una bala en el HUD (badge violeta pulsante en el tab de duelo) que expira a medianoche: entrenar → cargar → disparar → push al rival, el loop diario de 90 segundos hecho cuerpo. La regla 'entrena hoy para desbloquear tu ataque' ya existe en el RPC; hacerla visible convierte el hábito en gatillo y el PvP en razón para volver mañana. | LA CINTA DE LA DEUDA — cuando rompes el trato, tu home viste la cinta diagonal de peligro hasta que tu compa (el acreedor, nunca tú) te absuelve marcando el castigo como cumplido; saldarla dispara la única celebración que genera share card directo a WhatsApp. La deuda visible-pero-saldable es 'lo prometido es deuda' renderizado, y cada deuda saldada compartida recluta al siguiente dúo.

████ EXPERTO: ARTE DE UI ████
VEREDICTO: dovo tiene un backend de videojuego de verdad —ataques con munición que se gana entrenando, escudos consumibles con revelado variable, clases con lore, seis stats tokenizados— y una UI que se avergüenza de él. El tier más aspiracional del sistema (Legend) se renderiza a 9px y 30% de opacidad: eso no es restraint, es sabotaje. El "vs" del duelo —el corazón del conflicto— está al 30%. Lanzar un golpe tiene la misma jerarquía visual que un toast de "guardado". Y el momento canónico de cualquier RPG, subir de nivel, es literalmente indetectable: crearCheckin devuelve {puntos} y nada más; el cliente jamás se entera de que cruzó el umbral. No existe UNA ceremonia en toda la app.

La buena noticia: no hay que demoler, hay que subir el volumen. El chrome editorial es un activo — es exactamente el "piano glass" de Marvel Snap: vidrio negro desaturado donde lo único que brilla es el juego. El error no es el restraint del marco; es que el contenido del juego heredó el mismo restraint. El violeta se usa como tinta de subrayado cuando debería ser combustible. Los números —el corazón de un juego— se comportan como pies de foto.

La dirección que dicto: "mesa de apuestas nocturna". Tres temperaturas emocionales con token propio (violeta #6d4aff = la casa, teal #3ac49a = pacto, rojo #ff3d5e = deuda/rivalidad), números protagonistas estilo Balatro que siempre cuentan y nunca aparecen de golpe, geometría diagonal SOLO en superficies de rivalidad, tiers como material (foil, borde cónico) en vez de microtexto, y un HUD de pulgar que reemplace ese menú de revista de 9px. La infraestructura ya existe —10 keyframes propios, espectro de 6 stats theme-reactive, reduced-motion bien resuelto— pero está al 10% del volumen. Súbanlo donde importa: check-in, ataque, level-up, victoria. La navegación se queda calma. El juego, no.

DIRECTIVAS:
[P0] Tokenizar la superficie de juego y las tres temperaturas emocionales
  En @theme de globals.css: (1) --surface-game: radial-gradient(130% 150% at 12% 0%, #16132a 0%, #0b0a14 55%, #07060d 100%) y --shadow-game: 0 24px 60px -28px rgba(109,74,255,0.55) como utility .card-game — reemplaza las 7 copias inline. (2) Tres tokens de modo: --color-coop: #3ac49a (dark #4adfb2) para boost/dúo/racha; --color-rival: #ff3d5e (dark #ff5c77) para ataque/duelo/deuda; --color-gold: #f0c75a para ceremonia/milestone. El violeta #6d4aff queda como 'la casa' (identidad, navegación, XP). Regla dura: el chrome (nav, fondos, formularios) permanece desaturado piano-glass; el color saturado vive SOLO en números, barras, cartas y eventos. (3) Easings de juego: --ease-spring: linear(...) generado (kvin.me/css-springs) con fallback @supports a cubic-bezier(0.34,1.56,0.64,1); --ease-snap: cubic-bezier(0.2,0.8,0.2,1). Overshoot solo en transform, nunca en color/opacity.
  → app/globals.css (@theme); migrar inline styles de CharacterCard.tsx, DuoChampion.tsx, home-authed.tsx, DuelScoreboard.tsx, ProBadge.tsx, ShowcaseCharacterCard.tsx, recompensas/page.tsx, nutricion/page.tsx
[P0] Sistema numérico protagonista: ningún número aparece, siempre cuenta
  Componente <GameNumber> único: odometer a mano (~60 líneas, columnas de dígitos con translateY vía WAAPI, compositor, aria-label con valor real) con count-up rAF 600–900ms easeOutExpo y font-variant-numeric: tabular-nums OBLIGATORIO. Peso 800–900 Geist. Escala por magnitud: transform: scale(clamp(1, 1 + var(--val)/1000, 1.3)). Glow por temperatura: text-shadow: 0 0 28px color-mix(in oklch, var(--mode-color) 55%, transparent) — violeta en XP/nivel, teal en boosts, rojo en marcador rival. Aplicar a: nivel del CharacterCard, marcador del DuelScoreboard (ya tiene useCountUp — generalizar), '+N pts' del check-in, racha, XP del perfil. Reduced-motion: salta al valor final.
  → nuevo app/_components/GameNumber.tsx; CharacterCard.tsx, DuelScoreboard.tsx, CheckinRow.tsx, perfil/page.tsx, recompensas/page.tsx
[P0] AppNav → HUD: bottom tab bar con badges de estado
  Matar los 7 links mono de 9px con overflow-x-auto. Bottom bar fija: height 64px + env(safe-area-inset-bottom), background rgba(7,6,13,0.85) + backdrop-filter: blur(12px), border-top 1px rgba(255,255,255,0.08). 5 slots con icono SVG 24px + label mono 9px: hoy / duelo / tabla / premios / perfil (ajustes migra a perfil). Activo = icono filled + dot 4px violeta debajo. BADGES de juego (la nav ES el HUD): munición lista = dot teal 8px en 'duelo'; ataque recibido sin ver = badge 16px bg --color-rival con count y anim-pop al montar; reto propuesto = dot violeta en 'duelo'. Top bar se adelgaza a 48px: mark de dos discos + chip de racha permanente (ver directiva de racha). Light pages mantienen la barra oscura: el HUD es siempre nocturno.
  → app/_components/AppNav.tsx (rehacer), nuevo app/_components/BottomHud.tsx, app/(app)/layout.tsx (padding-bottom 80px), server: query ligera de eventos no vistos
[P0] Pipeline de celebración: crearCheckin debe contar la historia completa
  Habilitador de todo lo demás: crearCheckin devuelve { puntos, deltas: Record<stat,number>, nivelAntes, nivelDespues, tierUps: {stat, tierNuevo}[], rachaSemanas, municionLista: boolean }. Con eso, capas de celebración con gating estricto (lección Duolingo): capa S (cada check-in, <400ms): squash del botón + '+N pts' flotante existente + los deltas de stat aparecen como chips '+12 FUE' en el color del stat, stagger 40ms. Capa M (tier-up, 600–1200ms): flash del color del stat en la barra + overshoot scaleX 1.04. Capa L (level-up, raro, 1.5–3s): <dialog>.showModal() con ::backdrop blur(8px) vía @starting-style + transition-behavior: allow-discrete; fases: anticipación 400ms (anillo conic-gradient via @property --p 0%→100%) → hit-stop 80ms → clímax (número de nivel scale(3)→1 con --ease-spring 450ms + burst DOM 16–20 partículas transform/opacity, cero deps) → settle (barra XP nueva + clase + CTA compartir). Reduced-motion: dialog estático con el resultado.
  → lib/actions/checkins.ts, CheckinRow.tsx, nuevo app/_components/LevelUpDialog.tsx, app/globals.css (keyframes burst + @property --p)
[P1] AttackPanel como consola de combate: nada rival está a 90°
  Contenedor con clip-path: polygon(0 0, 100% 2%, 99% 100%, 1% 97%) y header con cinta de peligro: repeating-linear-gradient(45deg, color-mix(in oklch, var(--color-rival) 18%, transparent) 0 8px, transparent 8px 16px). Botones de golpe/congelar dejan de ser pills de 11px: botón físico 100% width, height 52px, bg --color-rival, box-shadow: 0 5px 0 color-mix(in oklch, var(--color-rival), black 40%); :active { translate: 0 4px; box-shadow: 0 2px 0 ... }. Hit-stop: al confirmar el RPC, congelar 90ms (setTimeout) y soltar TODO el feedback junto (shake del card rival ±4px 250ms + '−10' flotante + buzz navigator.vibrate?.(15) Android-only). Revelado de escudo con tensión: 800ms de shake del banner ANTES de mostrar 'bloqueado' + anim-shield-flash existente. Countdown de congelamiento: congela_hasta ya existe — chip mono 'descongela en 7h 32m' con tabular-nums. Munición: deja de ser texto 10px → indicador de 1 slot tipo cargador (disco lleno teal = lista, vacío con 🔒→icono candado SVG = entrena hoy).
  → app/(app)/retos/_components/AttackPanel.tsx, DuelFeed.tsx, app/globals.css (keyframes danger-tape, clip-path utilities)
[P1] DuelScoreboard con tensión real: dos lados, dos temperaturas
  Tu lado: número con glow violeta; lado rival: glow --color-rival. El 'vs' al 30% muere: chip diagonal rotate(-8deg), bg rgba(255,255,255,0.1), border 1px rgba(255,255,255,0.2), padding 4px 10px, mono 12px. Barra de ventaja central: track h-2 rounded-full bg-white/10; dos fills con transform: scaleX desde el centro (transform-origin center), transition 700ms --ease-snap, violeta vs rojo. Estado 'vas perdiendo': tu número con filter: grayscale(0.4) + label mono 11px color --color-rival 'vas abajo' + el delta exacto ('te faltan 47'). Estado 'vas ganando': pulso sutil del glow (opacity del text-shadow 0.45↔0.6, 3s). Al cambiar el marcador (revalidación): el número que subió hace anim-pop. Cierre de duelo deja de ser botón gris: CTA físico dorado si ganaste, rojo si perdiste (ver ceremonia W/L).
  → app/_components/DuelScoreboard.tsx, app/(app)/retos/page.tsx
[P1] CharacterCard como carta coleccionable: el tier es material, no microtexto
  (1) Tiers legibles: de text-[9px] white/30 → 11px mono, color del stat al 100% (--stat-* dark variants pasan AA sobre #07060d). (2) Tier como tratamiento de borde de cada barra/chip: Rookie = 1px rgba(255,255,255,0.12); Athlete = 2px solid var(--stat); Expert = 2px + drop-shadow(0 0 12px color-mix(in oklch, var(--stat) 40%, transparent)); Master = foil: border-image linear-gradient(120deg, var(--stat), #fff 30%, var(--stat) 60%) + background-position animado 8s; Legend = borde cónico: wrapper con padding 2px y background conic-gradient(from var(--angle), var(--stat), #fff, var(--stat)) rotando 6s vía @property --angle (paint acotado a chips de 48–96px, nunca full-card). Prestige 'Immortal' = tratamiento negative solo en el número de nivel: filter: invert(1) hue-rotate(180deg) sobre su chip. (3) Barra de XP EN LA HOME (hoy solo está en perfil): dos capas estilo fighting-game — fill principal scaleX con --ease-spring + ghost bar rgba(255,255,255,0.25) que drena con delay 400ms / 800ms linear: el ojo lee cuánto ganaste. (4) Racha: de '{racha} sem ✦' 11px white/45 → chip propio con icono llama SVG y número tabular en --color-coop. (5) Glare idle: capa radial-gradient con mix-blend-mode: overlay que 'respira' 6s (no touch-tracking en móvil).
  → app/_components/CharacterCard.tsx, ShowcaseCharacterCard.tsx, StatBar.tsx, home-authed.tsx, app/globals.css (@property --angle, keyframes foil/glare)
[P1] Sistema de iconos SVG propio: los emojis se retiran
  Un solo archivo icons.tsx con sprite <symbol> 24×24, stroke 2px, esquinas geométricas (el lenguaje del mark de dos discos: círculos y cortes), variante filled para estados activos. 12 glifos: golpe (puño geométrico), hielo (hexágono facetado), escudo, chispa (boost, reemplaza ✦), llama (racha), munición (disco), nivel (chevrón doble), duelo (dos discos enfrentados), premio (cofre), candado, cámara, corona (champion). Color vía currentColor para heredar temperatura. Los emojis 🥊❄️🛡️💥⚡🔒📸✦ desaparecen de superficies de juego (pueden quedarse en copy conversacional). El mark de dos discos además se vuelve ornamento de marco: separadores y bullets vía mask-image con el SVG del mark, 16px, opacity 0.3.
  → nuevo app/_components/icons.tsx; AttackPanel.tsx, DuelFeed.tsx, BoostButton.tsx, perfil/page.tsx, recompensas/page.tsx, BottomHud.tsx
[P1] Boost como ritual cooperativo teal + fix de copy falso
  BoostButton deja de ser link de texto enterrado: card 100% width, bg color-mix(in oklch, var(--color-coop) 12%, transparent), border 1px color-mix(... 40%), icono chispa SVG teal. Al enviar: anim-pop + halo box-shadow 0 0 0 0 → 0 0 0 8px color-mix(coop 35%) → fade 600ms. Recibirlo (home): banner con borde teal y el mark de dos discos haciendo dv-breathe (la animación de marca migra del landing a la app — el dúo respira cuando coopera). Escudo en mano SIEMPRE visible: chip 'escudo activo · 5d' con icono escudo teal en home Y en /retos (hoy el dueño casi no lo sabe). BUG DE COPY: hintEscudo dice 'protege la racha esta semana' — FALSO; corregir en es.json/en.json a 'bloquea el próximo ataque rival' / 'blocks the next rival attack'.
  → app/_components/BoostButton.tsx, home-authed.tsx, app/(app)/retos/page.tsx, messages/es.json, messages/en.json
[P2] Chip de racha permanente con estado de riesgo (loss aversion visual)
  Chip en la top bar del HUD, visible en TODA pantalla: icono llama + número tabular. Tres estados: (1) sana = llama --color-coop, número blanco; (2) en riesgo (compliance semanal incompleta y quedan ≤2 días — la data rutina+checkins ya existe sin UI) = filter: grayscale(0.8) + animation pulse 2s (opacity 0.55↔1) + contador 'vas 2/4'; (3) recién subida = anim-pop + flash teal una vez. Tap → bottom-sheet con detalle de compliance de AMBOS miembros ('Ana va 3/4, tú vas 2/4'). Es el gancho de retención más fuerte del modelo y hoy es invisible. Reduced-motion: estado gris estático con el texto 'en riesgo'.
  → nuevo app/_components/StreakChip.tsx, BottomHud.tsx/AppNav.tsx, server: cálculo de compliance semanal (datos existentes en rutina + checkins)
[P2] Ceremonia de victoria/derrota de duelo + revancha
  Al cerrar duelo, takeover <dialog>: revelado clip-path: circle(0% at 50% 40%) → circle(140%) en 500ms --ease-snap. Victoria: letter grade 'W' (o S/A/B según margen) a 140px, Geist 900, -webkit-text-stroke 2px --color-gold, entrada scale(3)→1 con --ease-spring, fondo .card-game con foil dorado (linear-gradient 120deg animado), marcador final con GameNumber, y head-to-head persistente: 'van 3–2' (la rivalidad se fabrica con historial + resultados cerrados). Derrota: 'L' con stroke --color-rival, franja de peligro 45°, y el castigo del trato en primer plano ('lo prometido es deuda'). CTA doble: [compartir] (reusar estética .share-mock 9:16 del landing — ya es un 'drop de loot') + [revancha] que pre-llena el form de reto con el mismo rival. Sin esto, semanas de competencia mueren en un router.refresh().
  → nuevo app/(app)/retos/_components/DuelResultDialog.tsx, retos/page.tsx, lib/actions/retos.ts (head-to-head count), landing.css → extraer .share-mock a compartido
[P2] Limpieza de identidad: fuentes muertas, grain selectivo, tokens compartidos
  (1) Eliminar Inter y JetBrains Mono de layout.tsx (peso muerto en Google Fonts; Geist/Geist Mono/Syne se quedan). (2) Grain.tsx (el 'tell' de imprenta, 4.5% soft-light) se queda SOLO sobre superficies papel claras; excluirlo de .card-game y del HUD — el vidrio del casino no lleva grano de imprenta. (3) landing.css deja de duplicar tokens bajo .dv-landing: consumir las vars de @theme (un solo origen de verdad para signal/papel/ink). (4) Actualizar BRAND.md y DESIGN.md a esta dirección ('mesa de apuestas nocturna', tres temperaturas, mark de dos discos como invariante) — hoy se contradicen entre sí y con el código. (5) Antes de tocar CharacterCard.tsx: git status (sesiones paralelas detectadas en el repo).
  → app/layout.tsx, app/_components/Grain.tsx, app/_components/landing.css, BRAND.md, docs/specs/DESIGN.md

FIRMAS: El mark vive: las dos 'o' de dovo como personaje de estado en el HUD — respiran en idle (dv-breathe migrada del landing), chocan y rebotan en éxito, se sacuden al recibir golpe, se escarchan al congelarse. La reacción de personaje de Duolingo sin Rive: un SVG + 4 clases de estado CSS. Ninguna otra app fitness tiene una marca que reacciona a tu duelo. | Tres tintas de temperatura: violeta #6d4aff = la casa (identidad, XP, navegación), teal #3ac49a = pacto (boost, racha, dúo), rojo #ff3d5e = deuda y rivalidad (ataque, duelo, castigo). Cualquier captura de pantalla se decodifica en un segundo: sabes si estás cooperando, peleando o cobrando — y el chrome piano-glass desaturado hace que las tres tintas griten. | Nada rival está a 90°: toda superficie de duelo/ataque/deuda lleva corte diagonal (clip-path polygon 1–4%) y cinta de peligro a 45° en rojo; el resto de la app permanece ortogonal y calma. El ángulo ES el conflicto — es la firma Persona 5 traducida a 'lo prometido es deuda', y se reconoce hasta en un thumbnail de WhatsApp.

████ EXPERTO: PSICOLOGÍA CONDUCTUAL ████
VEREDICTO: El diagnóstico conductual es simple y grave: dovo tiene los cuatro pasos del hook construidos en el backend y tres de los cuatro rotos en la UI. La ACCIÓN funciona — check-in a un tap con "+N pts" flotante es correcto y se queda. Todo lo demás está muerto. TRIGGER: el disparador interno natural de un producto de dúos es "¿qué hizo el otro?" — ¿ya entrenó?, ¿me atacó?, ¿me boosteó? — y hoy esa pregunta no tiene NINGUNA superficie: la víctima de un golpe no ve nada en home ni en nav, el estado del compañero es invisible, y la nav de 7 links de 9px no tiene un solo badge. RECOMPENSA VARIABLE: el sistema ya posee la mejor variabilidad posible — el escudo de revelado incierto, el estado del otro, el umbral de nivel que no sabes si cruzarás — y la UI la aplana a texto instantáneo: el reveal del escudo dura 0ms, crearCheckin devuelve solo {puntos} y el level-up es literalmente indetectable. INVERSIÓN: rachas, max_streak, niveles, prestige, historia del trato — todo almacenado, casi nada mostrado; abandonar dovo hoy se siente gratis porque la UI jamás enseña lo acumulado. Y la herida de marca: "lo prometido es deuda" no tiene ceremonia (sellar el pacto es un redirect mudo) ni libro contable visible.

En Octalysis, dovo debe correr sobre Social Influence (CD5), Loss & Avoidance white-hat (CD8), Accomplishment (CD2) y Scarcity/Unpredictability (CD6/7). Epic Meaning no aplica y no lo forzaremos. Líneas rojas innegociables que rechazaré en cualquier propuesta: fabricar o simular el estado del otro ("tu rival está entrenando ahora" falso), notificaciones de culpa, absolución comprable del castigo del trato (misericordia acotada SOLO en la capa de racha, nunca en el trato), scarcity inflada, daño-de-equipo estilo Habitica sin válvula de escape, y celebrar todo igual — el gating estricto es la técnica #1: si todo es jackpot, nada lo es. La confianza del dúo es el activo; cualquier manipulación que la erosione mata el producto dos usuarios a la vez.

DIRECTIVAS:
[P0] DuoPulse: el estado del otro como primer elemento de la home (trigger interno + recompensa variable de tribu)
  Crear componente server `DuoPulse` renderizado ANTES del héroe en home-authed.tsx. Son los dos discos del mark a 28px: el tuyo y el de tu compañero. Disco lleno con --c-signal (#6d4aff) + animación dv-breathe (3.6s, desfasada 1.8s entre discos) si ese miembro ya hizo check-in HOY; disco hueco (border 1.5px ink/25, filter grayscale(.8)) si no. Debajo, label mono 11px tracking 0.18em: 'tú ✓ · ana aún no' / 'los dos ya entrenaron hoy ✦'. Tap en el disco pendiente del compañero → nudge pre-escrito en voz MX vía push ('no me dejes morir solo, compa'), máximo 1/día, con confirmación. Esto convierte abrir la app en tirada variable: nunca sabes el estado del otro hasta entrar. Datos: check-ins de hoy ya disponibles server-side.
  → app/_components/DuoPulse.tsx (nuevo), app/_components/home-authed.tsx, app/globals.css (portar dv-breathe desde landing.css a tokens compartidos), messages/es.json + en.json
[P0] Bottom tab bar con badges: los triggers externos viven en la zona del pulgar
  Sustituir en móvil los 7 links de 9px del AppNav por bottom tab bar fija: nuevo client island `BottomNav`, position fixed bottom, altura 56px + env(safe-area-inset-bottom), bg-papel/85 + backdrop-blur, 5 tabs (hoy, retos, tabla, premios, perfil; nutrición y ajustes se anidan en hoy/perfil). Iconos: set SVG propio de 14px derivado del mark (NO emoji). Badges de evento: dot de 8px en --stat-flex (#c44aff) sobre 'retos' si hay ataque recibido no visto o reto propuesto; dot --c-signal sobre 'hoy' si hay boost recibido. aria-label con el conteo real ('retos, 1 ataque recibido'). AppNav superior queda solo con wordmark + ajustes en móvil. Sin badge, ningún trigger PvP llega al usuario: este es el fix del hueco #2 del recon.
  → app/_components/BottomNav.tsx (nuevo), app/_components/AppNav.tsx, app/(app)/layout (padding-bottom 56px en main)
[P0] Cerrar acción→recompensa: crearCheckin devuelve el delta completo y dispara celebración por capas
  Cambiar el retorno de crearCheckin a {puntos, deltas:{fue..vit}, nivelAntes, nivelDespues, tierUps:[{stat,antes,despues}], rachaSemanas, rachaSalvada}. CheckinRow consume por capas con gating estricto: capa S (siempre, lo existente): float '+N pts' + anim-pop. Capa M (solo tier-up): flash del color del stat en la barra del CharacterCard + overshoot scaleX 1.04 (700ms, ease-spring linear() tokenizado en @theme con fallback cubic-bezier(0.34,1.56,0.64,1)) + chip 'RES → Expert' 12px mono. Capa L (solo level-up): <dialog>.showModal() con @starting-style scale 0.8 → 1: anticipación 350ms (backdrop dim + nivel viejo), clímax (nivel nuevo entra scale(3)→1 en 450ms con overshoot, weight 900, textShadow violeta existente), settle (clase + CTA 'presumir' share card). Total ≤2.5s, cerrable al tap, prefers-reduced-motion: crossfade a estado final. La variabilidad es legítima: no sabes si ESTE check-in te sube de nivel, y se paga con esfuerzo real.
  → lib/actions/checkins.ts, app/_components/CheckinRow.tsx, app/_components/CharacterCard.tsx, app/globals.css (@theme: --ease-spring, --ease-bounce), nuevo app/_components/LevelUpDialog.tsx
[P0] PactoMeter: compliance semanal en vivo — Loss & Avoidance white-hat
  Componente en home bajo DuoPulse: 'vas 2/4 esta semana · ana va 3/3' como barra segmentada (segmentos de 8px llenándose con --c-signal, anim-bar-w existente). Tres estados: on-track (normal); en riesgo (jueves+ y faltan ≥2 sesiones): el contador de racha del dúo se desatura (filter grayscale(.8)) + pulso lento opacity 1↔0.75 de 2.4s (reusar anim-frost) + copy 'la racha del trato está en riesgo — aún están a tiempo'; salvada (cumplieron en la última sesión): flash del color + copy 'racha sellada ✦'. Los datos (rutina default + check-ins de la semana) ya existen y hoy NO tienen UI — es el gancho de retención más fuerte del modelo, invisible. Push de riesgo: opt-in explícito en ajustes, máximo 1/semana, copy jamás de culpa (prohibido 'vas a decepcionar a ana').
  → app/_components/PactoMeter.tsx (nuevo), app/_components/home-authed.tsx, app/(app)/ajustes (toggle push de riesgo), messages/es.json + en.json
[P0] Revelado del escudo con tensión: la slot machine diaria de dovo
  En AttackPanel, el resultado del ataque hoy es instantáneo — desperdicia la recompensa variable más pura del sistema (no sabes si tienen escudo). Nueva secuencia vía WAAPI con await .finished: tap → hit-stop 90ms (silencio total, nada se mueve) → fase de tensión 700ms: la carta/nombre del rival con wobble (keyframes rotate −1.2°→1.2°, 350ms ×2) → revelado: conectado = anim-shake existente + '−10' anim-float-away; bloqueado = anim-shield-flash + copy 'tenían escudo — se consumió el suyo'. Añadir countdown de congelamiento usando congela_hasta (hoy disponible y no renderizado): chip 'congelado · 7h 12m', refresh por minuto. prefers-reduced-motion: resultado inmediato en estado final, sin tensión animada. Nunca alargar más de 800ms: la tensión emociona, la espera frustra.
  → app/(app)/retos/_components/AttackPanel.tsx, app/(app)/retos/_components/DuelFeed.tsx (countdown), app/globals.css (keyframe wobble)
[P1] Boost reposicionado: la inversión que carga el trigger del otro + fix de copy falso
  Mover el trigger del boost al momento de máxima motivación: tras check-in propio exitoso, si hay boost disponible (racha ≥2, sin cooldown), la home muestra CTA contextual pill (no link de texto): 'ya entrenaste — dale energía a ana ✦'. Recibirlo deja de ser banner fino: card capa M (600-900ms) con nombre del emisor, glow del color del boost, y CTA de reciprocidad 'devuélvele el detalle' (deep-link a dar boost cuando esté disponible). La reciprocidad es el pegamento del dúo: cada boost dado es inversión que dispara el regreso del otro. CRÍTICO: corregir hintEscudo en los mensajes — hoy dice 'protege la racha esta semana' y es FALSO (el escudo bloquea ataques de duelo). Copy correcto: 'bloquea el próximo ataque rival'. Mentirle al usuario sobre mecánicas destruye la confianza que sostiene todo el sistema.
  → app/_components/BoostButton.tsx, app/_components/home-authed.tsx, app/_components/CheckinRow.tsx (estado post-éxito), messages/es.json + en.json (hintEscudo)
[P1] Ceremonia de cierre de duelo + head-to-head + revancha: la derrota debe cargar el siguiente loop
  Exponer head-to-head persistente por par de dúos en lib/actions/retos.ts ('van 3–2') y mostrarlo como listón permanente en el header del DuelScoreboard y en el historial. Al cerrar duelo: <dialog> capa L — 'W'/'L' (o 'ganaron'/'perdieron') a 120-150px weight 900 con -webkit-text-stroke violeta, entrada scale(3)→1 con overshoot cubic-bezier(.2,1.6,.4,1) en 400ms. Victoria: confetti canvas ≤80 partículas (import() dinámico, disableForReducedMotion) + share card. Derrota: CERO burla — marcador final + head-to-head + CTA primario 'revancha' que pre-llena un reto nuevo contra el mismo rival (Kilduff: la rivalidad se fabrica con competencia repetida y resultados cerrados). Añadir badge/banner cuando el duelo venció y falta cerrarlo. La derrota sin siguiente paso = churn; la derrota con revancha = rivalidad.
  → lib/actions/retos.ts, app/_components/DuelScoreboard.tsx, app/(app)/retos/page.tsx, nuevo app/(app)/retos/_components/DuelResultDialog.tsx
[P1] Sello del pacto: el momento de marca deja de ser un redirect mudo
  El pico de motivación (Fogg) es crear el trato, y hoy termina en router.push('/') seco. Insertar pantalla de sello de 1.6-2s antes del redirect: los dos discos del mark entran desde lados opuestos (translateX ±60px → 0, 600ms ease-spring), quedan en paralelo respirando (dv-breathe), y debajo aparece 'lo prometido es deuda' en mono tracking 0.18em con fade 400ms; CTA 'empezar'. Segundo momento crítico (cuello de botella del funnel según los datos de Friend Streak): cuando el partner acepta el invite, el creador DEBE enterarse — push + banner capa M en home 'ana selló el trato ✦' con los dos discos llenos. Hoy el creador nunca ve cuando su dúo nace: el loop de invitación muere sin cierre emocional.
  → app/(app)/onboarding/* (grupo), app/(app)/invite/[token] (JoinButton), app/_components/home-authed.tsx (banner partner-unido), push notification en la action de join
[P1] Expediente del trato: la inversión acumulada visible que hace caro abandonar
  Bloque 'el trato' en /grupo/[id] con estética de libro contable (encaja con el ADN editorial Y sirve a la psicología): fecha del pacto, racha actual, MEJOR racha (max_streak — existe en DB y jamás se muestra), semanas selladas totales, head-to-head de duelos W-L, boosts intercambiados. Layout: filas hairline existentes (border-ink/10, rounded-xl) + números display tabular-nums; el récord de racha con tratamiento foil sutil (gradiente animado con background-blend-mode color-dodge, ciclo idle de 6s, estático en reduced-motion). Cada dato es endowment: lo que el dúo ya construyó. Sin esto, romper el trato se siente gratis; con esto, cada semana acumulada es un argumento silencioso para volver mañana.
  → app/(app)/grupo/[id]/page.tsx, lib/actions (exponer max_streak personal y de dúo), app/globals.css (utility foil)
[P2] Munición como objeto escaso visible (scarcity honesta, CD6)
  Sustituir el texto de 10px con 🔒 del AttackPanel por un indicador-objeto: disco de 16px lleno con --c-signal = 'tienes 1 golpe cargado · expira a medianoche'; disco hueco con borde dashed = 'entrena hoy para cargar tu ataque'. Mostrar también en home cuando hay duelo activo (junto a DuoPulse), porque la munición es el acople hábito→PvP más elegante del sistema y hoy está enterrada en /retos. Regla ética: la scarcity es real (1 ataque/miembro/día, enforced en RPC) — comunicarla tal cual, jamás inflarla ni inventar urgencia. Al cargar munición tras el check-in: pop del disco (anim-pop existente) como recompensa secundaria del check-in.
  → app/(app)/retos/_components/AttackPanel.tsx, app/_components/home-authed.tsx, app/_components/DuoPulse.tsx
[P2] Reclamar recompensas con card flip: del estado pasivo al acto de posesión
  En recompensas, el desbloqueo hoy es pasivo (la card cambia de gris a violeta en el siguiente render): cero endowment. Nuevo flujo: al alcanzar el threshold la card entra en estado 'lista para reclamar' (borde pulsante --c-signal 2s + badge mono 'reclamar') y NO se revela sola; el usuario la voltea: anticipación 600ms (wobble suave) → rotateY(180deg) en 600ms con backface-visibility:hidden → frente revelado con flash del color + check. prefers-reduced-motion: crossfade directo al estado revelado. El acto físico de reclamar convierte el premio en posesión (efecto dotación) y da a la pantalla de recompensas su único momento de juice — gateado por rareza real (thresholds 2/4/8/12/24/52 semanas).
  → app/(app)/recompensas/page.tsx, nuevo client island RewardClaimCard.tsx, app/globals.css (keyframes flip + pulse-border)
[P2] El día después de la racha rota: diseñar contra el what-the-hell effect
  El 90% abandona apps de hábito al romper una racha. Si la racha del dúo se rompe, la home muestra UNA SOLA VEZ (flag visto) una card de duelo asumido: 'se rompió la racha de 8 semanas' + el récord preservado en grande ('su mejor marca: 11 sem — sigue ahí', tabular-nums, sin desaturar) + CTA primario 'empezar la revancha' que arranca racha nueva con micro-meta visible de 1 semana (barra segmentada del PactoMeter reseteada pero con el récord como línea de meta fantasma). Prohibido: pantalla de luto recurrente, push de culpa, contador en cero sin contexto. La inversión preservada (récord, niveles, expediente) es lo que trae de vuelta al dúo; mostrar solo la pérdida lo expulsa. Misericordia acotada vive aquí — en la racha — y JAMÁS en el castigo del trato.
  → app/_components/home-authed.tsx (card racha-rota), app/_components/PactoMeter.tsx (línea de récord fantasma), lib/actions (flag de racha rota vista), messages/es.json + en.json

FIRMAS: El mark como latido del dúo: los dos discos dejan de ser logo y se vuelven el organismo de estado central — respiran (dv-breathe) cuando cada miembro ya entrenó hoy, se apagan en gris cuando no, entran desde lados opuestos y se alinean al sellar el pacto, y pulsan desaturados cuando la racha peligra. La marca ES la superficie del trigger: nadie más puede copiar eso. | El revelado del escudo como ritual de apuesta diaria: cada ataque abre 90ms de hit-stop + 700ms de tensión (wobble de la carta rival) antes de saber si conectó o lo bloquearon — la única slot machine de dovo, perfectamente ética porque la moneda se gana entrenando de verdad y la incertidumbre es real (nunca sabes si el rival tiene escudo). | El libro contable del trato: 'lo prometido es deuda' renderizado literalmente como expediente — fecha del pacto, récord de racha en foil, head-to-head 3–2, semanas selladas, boosts intercambiados. La estética de ledger editorial puesta al servicio de la psicología de inversión: cada fila es una razón para no romper y un trofeo para presumir.

████ EXPERTO: SISTEMAS / LEGIBILIDAD ████
VEREDICTO: dovo tiene un sistema de juego mejor que el 90% de las fitness apps y una interfaz que lo niega. Bajo el lente MDA las mecánicas son sólidas — economía dual (puntos_base vs puntos boosteados), munición que se gana entrenando, escudos consumibles, dos rachas con roles distintos — y las dinámicas que producen (PvP que alimenta el hábito, cooperación gateada por compromiso) son exactamente las que un commitment device necesita. Pero la capa estética transmite "revista" y, peor, el sistema es ILEGIBLE: el jugador no puede formar un modelo mental de las reglas mirando la pantalla. Hoy las reglas se descubren tropezando con errores P0001 crudos ("entrena hoy para desbloquear tu ataque" DESPUÉS del tap — la definición de affordance falsa). El estado más importante del meta-juego (compliance semanal del dúo, que decide la racha que gatea TODO: boosts y recompensas) no tiene ni un pixel. El recurso más elegante del diseño (munición = check-in de hoy) vive en texto de 10px con un candado emoji. El dueño de un escudo no sabe que lo tiene. La escalera aspiracional del RPG (tiers Master/Legend) se renderiza a 9px al 30% de opacidad — el sistema esconde su propia zanahoria. Y la UI miente activamente: hintEscudo promete "proteger la racha" (falso, bloquea ataques), la economía dual jamás se etiqueta (un usuario boosteado verá el leaderboard "roto" y perderá confianza en los números), y los cooldowns son invisibles hasta que fallan. El lente GMTK es despiadado aquí: una mecánica que el jugador no puede leer NO EXISTE — y dovo tiene cinco mecánicas excelentes en ese limbo. No necesita más sistemas: necesita un VOCABULARIO. Un icono, un color y un lugar fijo por recurso; estados bloqueados que muestran su requisito en vez de esconderse; y que cada acción devuelva un recibo legible de lo que produjo. El backend ya habla como videojuego; la UI lo traduce a pie de foto. Eso se corrige con tokens, un set de iconos SVG y cambiando el payload de una server action — no con un rediseño de meses.

DIRECTIVAS:
[P0] Léxico de recursos: un recurso = un icono + un color + un lugar, SIEMPRE
  Tokenizar en @theme los 5 recursos del juego y prohibir su uso para otra cosa: munición → --c-ammo: var(--color-stat-vit) (lima #aef03c); escudo → --c-shield: var(--color-stat-equ) (teal #3ac49a, ya canónico vía anim-shield-flash); congelamiento → --c-freeze: var(--color-stat-vel) (cyan #3ac4d6); energía/boost → --c-energy: var(--color-signal); racha de dúo → --c-racha NUEVO (#b86a10 light para texto AA sobre papel, #ffb454 dark) — único color nuevo del sistema, estilo Duolingo: la racha SIEMPRE ámbar en toda la app. Reemplazar TODOS los emojis (🥊❄️🛡️⚡🔒✦) por un set SVG inline de trazo 2px en componente GameIcon (props: name, size 16|20|24): cápsula/bala = munición, hexágono = escudo, copo 6 puntas = congelamiento, dos eslabones = racha del trato (la cadena de 'lo prometido es deuda'), rayo = energía, mark de dos discos = XP/nivel. FLE magenta #c44aff queda reservado exclusivamente al rival.
  → ~/dovo/app/globals.css (@theme), nuevo ~/dovo/app/_components/GameIcon.tsx; sustitución en AttackPanel.tsx, DuelFeed.tsx, BoostButton.tsx, CharacterCard.tsx, app/(app)/recompensas/page.tsx
[P0] BottomHUD: tab bar en la zona del pulgar con contadores de recursos
  Sustituir los 7 links mono de 9px con overflow-x-auto por una bottom tab bar fija de 64px + safe-area-inset-bottom a 390px: 5 tabs (hoy, duelo, tabla, premios, perfil) con icono 24px + label mono 9px. Slot derecho permanente de recursos: chip de munición (encendido: lima + box-shadow 0 0 10px var(--c-ammo) si entrenaste hoy; apagado: filter grayscale(1) opacity .4 si no) y hexágono de escudo si está en mano. Badges de evento: dot 8px --c-freeze sobre 'duelo' si estás congelado, dot magenta si recibiste golpe en <24h, dot signal sobre 'premios' si hay recompensa sin reclamar. El nav superior queda solo con wordmark + ajustes. Este HUD es la condición para que cualquier otra mecánica sea legible: estado del jugador visible en TODA pantalla.
  → ~/dovo/app/_components/AppNav.tsx → nuevo ~/dovo/app/_components/BottomHUD.tsx; layout en ~/dovo/app/(app)/
[P0] crearCheckin devuelve el recibo completo — y la UI lo imprime
  Cambiar el retorno de {puntos} a {puntos, boostAplicado, deltas:{fue..vit}, nivelAntes, nivelDespues, xpParaSiguiente, tierUps:[{stat,de,a}], rachaPersonal, municionLista}. Sin este payload el level-up es físicamente indetectable en cliente. CheckinRow al completar expande un 'recibo de esfuerzo' 2.5s: chips de delta por stat en su color ('+82 fue' con bg color-mix(in oklch, var(--stat-fue) 12%, transparent)), barra de XP de dos capas (fill scaleX con --ease-spring 450ms + ghost bar que drena con delay 400ms / 800ms linear — patrón provisional-damage), y el '+N pts' flotante existente. Si hubo boost: etiqueta inline '+40 boost · solo personaje' en --c-energy. Si nivelDespues>nivelAntes o tierUps no vacío → disparar la celebración capa L (el dato es de este consejo; el juice, del de game-feel). La barra de XP de dos capas se replica en CharacterCard del home, no solo en /perfil.
  → ~/dovo/lib/actions/checkins.ts, ~/dovo/app/_components/CheckinRow.tsx, ~/dovo/app/_components/CharacterCard.tsx
[P0] 'Pacto de la semana': compliance del dúo visible en home
  Widget arriba del CharacterCard: dos filas (tú / tu compa), cada una con N segmentos = frecuencia de su rutina (pills de 6px alto, llenos en signal, vacíos border-ink/15) + contador '2/4' tabular-nums + chip de racha del trato (eslabones, --c-racha) a la derecha con el número de semanas. Estado en riesgo (faltan más check-ins que días restantes de la semana, o jueves+ con ≥2 pendientes): la racha se desatura filter grayscale(.8) + pulso de opacity 2.4s (reusar timing de anim-frost) + copy 'la racha del trato está en riesgo'. Si el compa va atrás: CTA 'recuérdale' (nudge en voz MX). Data 100% calculable hoy server-side (rutina default + check-ins de la semana). ADVERTENCIA: verificar cómo corre cerrar_semana_rachas en prod (no hay scheduler en el repo) antes de prometer el tick semanal en UI.
  → nuevo ~/dovo/app/_components/PactoSemanal.tsx montado en ~/dovo/app/_components/home-authed.tsx; query en ~/dovo/lib/
[P0] Cazar las cuatro mentiras de la UI
  (a) hintEscudo en BoostButton dice 'protege la racha esta semana' — FALSO: cambiar a 'bloquea el próximo ataque rival por 7 días'. (b) AttackPanel deriva los estados del botón ANTES del tap, jamás después: sin munición = botón apagado (grayscale) con 'entrena hoy para cargar tu golpe'; ya atacaste = countdown 'próximo ataque en 14 h' mono tabular-nums; mapear todos los P0001 del RPC a estos estados — el string crudo del RPC nunca llega al usuario como sorpresa post-tap. (c) RETO_ACEPTAR_HORAS=48 declarado sin enforcement: o se implementa o se borra de constants — cero promesas en UI sin mecánica detrás. (d) Regenerar lib/supabase/types.ts (aún lista castigo_text del modelo v0 dropeado) para que nadie diseñe contra un esquema muerto.
  → ~/dovo/app/_components/BoostButton.tsx, ~/dovo/app/(app)/retos/_components/AttackPanel.tsx, ~/dovo/lib/actions/ataques.ts, ~/dovo/lib/scoring/constants.ts, ~/dovo/lib/supabase/types.ts
[P1] Tiers como material, no como pie de foto
  En CharacterCard/StatBar subir el tier de text-[9px] white/30 a 11px mono al 100% de opacidad con tratamiento de superficie por rango: Rookie = ink-soft plano; Apprentice/Athlete = color del stat al 70%; Expert = color pleno; Master = borde 2px del color del stat + drop-shadow del mismo al 40%; Legend = borde conic-gradient rotando 6s (en reduced-motion: gradiente fijo). Mostrar el número display (0–150) junto al tier en tabular-nums 11px y, en /perfil, una mini-escala de 6 puntos bajo cada barra (Rookie→Legend) con el punto actual lleno — el jugador debe leer 'qué tan lejos está Legend' sin manual. Tokens --tier-ring-* en @theme para no duplicar inline.
  → ~/dovo/app/_components/CharacterCard.tsx, ~/dovo/app/_components/StatBar.tsx, ~/dovo/app/globals.css, ~/dovo/app/(app)/perfil/page.tsx
[P1] Scoreboard que responde: ¿quién gana, por cuánto, y qué altera el flujo?
  DuelScoreboard: (1) barra de ventaja central de 6px — dos scaleX enfrentados desde el centro, lado propio var(--color-signal), rival var(--color-stat-fle); (2) reemplazar el 'vs' al 30% por el diferencial vivo ('+23') al 100% de opacidad del lado que domina, animado con el useCountUp existente; (3) línea de estado mono 11px sin eufemismos: 'van ganando' / 'vas abajo por 23'; (4) miembro congelado = anillo conic-gradient(var(--c-freeze) var(--p), transparent 0) de 20px alrededor de su avatar que se consume hacia 0 usando congela_hasta (transición de --p vía @property, paint barato a ese tamaño) + countdown '9 h' tabular-nums. Un estado con duración pero sin reloj es ilegible; hoy 'congelado' es un chip sin tiempo.
  → ~/dovo/app/_components/DuelScoreboard.tsx, ~/dovo/app/(app)/retos/_components/DuelFeed.tsx
[P1] Estados bloqueados que enseñan: el onboarding ES el candado
  Regla global: una mecánica gateada NUNCA se oculta — se muestra bloqueada con requisito y progreso exactos. BoostButton pre-gate: candado SVG + 'se desbloquea con racha del trato de 2 sem (vas 1/2)'. Cooldown: 'disponible en 3 días'. Recompensas ya lo hace bien (grayscale + barra) — es el patrón a extender, no inventar. Primera activación de cada recurso = tooltip de UNA línea, UNA vez (flag localStorage dovo_seen_{recurso}): munición ('entrenaste hoy → tienes 1 golpe'), escudo recibido ('tu compa te cubrió: el próximo ataque rebota'), congelado ('tus check-ins no suman al duelo por 12 h — tus stats sí crecen'). Capas naturales: semana 1 solo check-in/stats visibles activos; racha 2 enciende boosts; primer duelo enciende ataques. Cero tutorial modal.
  → ~/dovo/app/_components/BoostButton.tsx, ~/dovo/app/(app)/retos/_components/AttackPanel.tsx, ~/dovo/app/(app)/recompensas/page.tsx, ~/dovo/app/_components/home-authed.tsx
[P1] Scouting de rivales en el picker de retos
  RetoNuevoForm: cada candidato pasa de 'botón con solo el nombre' a carta mini de 64px: nombre + clase (sustantivo en signal, 'The Titan') + top_stat con su color de espectro + racha del trato en --c-racha + head-to-head si hay historial ('van 2–1' — base del sistema de rivalidad). Todos esos datos YA vienen en getLeaderboard y hoy se descartan en el picker. Corregir el copy stale de es.json ('id del dúo rival' / 'pídele a tus rivales el id'). Un duelo es una apuesta de 7 días: nadie apuesta sin ver al rival, y los matchups cerrados (Kilduff) son los que fabrican rivalidades.
  → ~/dovo/app/(app)/retos/nuevo/RetoNuevoForm.tsx, ~/dovo/lib/actions/leaderboard.ts, ~/dovo/messages/es.json
[P2] Política de números: crudos vs abstractos, sin monedas sin etiqueta
  CRUDOS siempre (tabular-nums): pts del check-in, marcador y diferencial del duelo, munición (0/1), compliance n/m, cooldowns en horas, XP-para-siguiente-nivel, semanas de racha. ABSTRAER siempre: la fórmula kcal→BMR (un único tooltip en el primer check-in: 'tus pts = tu esfuerzo ajustado a tu cuerpo — parejo contra cualquier rival', nunca la matemática), los acumuladores crudos de stat (solo display 0–150 + tier), la curva cuadrática de niveles. Regla de oro de la economía dual: cada vez que un número boosteado difiera del base, etiquetar en línea ('solo personaje') — un número sin procedencia es una mentira en potencia, y el caso 'mi leaderboard no refleja mis puntos' es el primer ticket de soporte que esto previene.
  → ~/dovo/app/_components/CheckinRow.tsx, ~/dovo/app/_components/CharacterCard.tsx, ~/dovo/app/(app)/perfil/page.tsx, ~/dovo/app/(app)/leaderboard/page.tsx
[P2] Leaderboard con movimiento y tu fila anclada
  DuoLeaderRow: delta de posición vs el último cierre semanal — '▲2' en teal EQU, '▼1' en magenta FLE, '=' en ink-soft, mono 10px junto al rank (persistir rank_prev al cierre del periodo o calcularlo en el RPC del leaderboard). Fila propia con position: sticky bottom-0 + bg-papel/85 backdrop-blur cuando queda fuera del viewport, para que 'dónde voy' nunca requiera scroll. Un leaderboard sin deltas es una foto; con deltas es una carrera — y la carrera es lo que retiene.
  → ~/dovo/app/(app)/leaderboard/page.tsx, DuoLeaderRow, ~/dovo/lib/actions/leaderboard.ts (+ migración rank_prev si aplica)
[P2] Buffs activos con reloj: energía y escudo en mano, visibles para su dueño
  Boost de energía activo: chip con rayo --c-energy en home Y dentro de CheckinRow ('tu próximo check-in vale +50% · expira en 18 h', countdown tabular-nums) — hoy es un strip genérico que no dice qué acción lo consume ni cuándo muere. Escudo en mano: hexágono --c-shield persistente en el BottomHUD + línea en /retos del lado propio ('traes escudo: el próximo golpe rebota') — hoy el dueño de un escudo casi nunca sabe que lo tiene, lo que anula su valor estratégico y el momento de drama del bloqueo. Todo buff con duración lleva countdown; todo buff de un solo uso lleva contador de cargas.
  → ~/dovo/app/_components/home-authed.tsx, ~/dovo/app/_components/CheckinRow.tsx, ~/dovo/app/(app)/retos/page.tsx, BottomHUD

FIRMAS: El recibo de esfuerzo: cada check-in imprime un desglose itemizado durante 2.5s — chips de delta por stat en su color de espectro + barra de XP de dos capas con ghost-drain — convirtiendo la matemática kcal→BMR en un momento visible y propio. Ninguna fitness app te muestra qué compró tu sudor; dovo te da el ticket. | La bala del día: el chip de munición en el BottomHUD que se enciende en lima con glow al entrenar y se apaga en gris si no — el loop completo del producto (entrenar = poder golpear a tu rival) legible en un solo pixel persistente en la zona del pulgar, en todas las pantallas. | El pacto que pulsa: el widget de compliance de dos filas (tú y tu compa, segmentos que se llenan) donde la racha del trato —siempre ámbar, siempre con sus dos eslabones— se desatura y pulsa lento cuando alguien va atrás. 'Lo prometido es deuda' convertido en interfaz: la deuda se VE antes de cobrarse.
